public class RequiredFieldIndicatorAttribute : PropertyAttribute
{
    // 현재는 속성 자체에 추가 데이터가 필요 없지만,
    // 나중에 검사 조건을 위한 파라미터 등을 추가할 수 있습니다.
    // public string WarningMessage { get; private set; }
    // public RequiredFieldIndicatorAttribute(string message = "필수 항목입니다.") { WarningMessage = message; }
}
